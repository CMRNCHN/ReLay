// ActionDispatcher is defined in Sources/ReLayCore/ActionDispatcher.swift (the authoritative version).
// This file is intentionally empty to avoid redeclaration in the same module.
//
// The input pipeline's IntentDispatching protocol is satisfied by ActionDispatcher.shared,
// which routes all intents through SpatialStateCore — never SpatialEngine directly.
